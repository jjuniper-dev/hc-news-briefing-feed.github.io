name: Daily RSS Feed Health Check

on:
  schedule:
    - cron: '0 10 * * *'
  workflow_dispatch:

jobs:
  health-check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Quick sanity check
        run: echo "✅ Workflow is recognized and running on $(date)"