      - name: Notify on failure
        if: failure()
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: ${{ secrets.SMTP_USERNAME }}
          password: ${{ secrets.SMTP_PASSWORD }}
          subject: "🔴 Canadian RSS Feed Health – Failure on ${{ github.run_date }}"
          body: |
            The following Canadian feeds failed health checks:

            ${{ steps.health.outputs.Feed_Status }}

            View details: ${{ github.run_url }}
          to: spatialsensation@gmail.com
          from: "RSS Health Bot" <spatialsensation@gmail.com>
          secure: true